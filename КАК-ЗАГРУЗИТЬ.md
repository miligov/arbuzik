# 🍉 АРБУЗИК — слей фрукты

Physics-merge головоломка для VK Mini Apps. Роняешь фрукты, одинаковые сливаются и растут: черника → вишня → клубника → … → **арбуз**. Тот самый жанр, который взрывает соцсети: правила понятны за 3 секунды, а оторваться невозможно.

Клиент — **один файл** `index.html` (44 КБ), сборка не нужна.

```
merge-game/
├── index.html          ← игра (заливать в ВК)
├── arbuzik.zip         ← упакована для хостинга ВК
├── КАК-ЗАГРУЗИТЬ.md    ← этот файл
└── store-assets/       ← иконки, сниппет, скриншоты
```

---

## Загрузка (3 шага)

1. **dev.vk.com** → «Создать приложение» → платформа **VK Mini Apps**, тип **Игра**, название `Арбузик`.
2. Настройки → **«Хостинг»** → перетащить **`arbuzik.zip`** → «Опубликовать».
3. Открыть `https://vk.com/app<ВАШ_ID>` → проверить → статус **«Включено для всех»**.

**Рейтинг:** включите «Рейтинг игроков», тип — **по очкам**.
**Каталог:** материалы для карточки — в папке `store-assets/`.

---

## Почему этот жанр «залетает»

- **Порог входа нулевой.** Одно действие — отпустить палец. Объяснять не нужно.
- **Дофаминовая петля.** Каждое слияние — вспышка, звук, очки. Каскад даёт комбо ×4.
- **«Ещё один разок».** Партия 3–5 минут, проигрыш всегда «по своей вине» — тянет переиграть.
- **Соревнование.** Рекорд + рейтинг друзей + челлендж дня с одинаковой раскладкой у всех.
- **Виральность.** Кнопка «Похвастаться» постит рекорд на стену.

---

## Как играть

1. **Веди пальцем** — фрукт едет за тобой над банкой.
2. **Отпусти** — падает.
3. **Два одинаковых** соприкоснулись → слились в следующий по размеру.
4. **Каскад** подряд → комбо ×2, ×3, ×4 к очкам.
5. Куча выше **красной черты** дольше 2 секунд → конец.

### Цепочка из 11 фруктов
🫐 Черника → 🍒 Вишня → 🍓 Клубника → 🍇 Виноград → 🍊 Мандарин → 🍎 Яблоко → 🍐 Груша → 🍑 Персик → 🍍 Ананас → 🍈 Дыня → **🍉 Арбуз**

С неба падают только первые 5 — остальные добываются слияниями.

### Режимы
- **Классика** — бесконечно, свой рекорд.
- **Челлендж дня** — детерминированная последовательность: у всех игроков в один день **одинаковые фрукты**. Честное соревнование, обновляется каждые сутки.

---

## Что внутри

**Своя физика.** Verlet-интегрирование, 6 подшагов и 4 итерации решателя на кадр, пространственная сетка для широкой фазы. Фрукты честно катятся, стекаются в щели и упруго сминаются при ударе (squash-анимация). 60 тел считаются за ~160 мс на 3 секунды симуляции — на телефоне идёт гладко.

**Звук без файлов.** Всё синтезируется WebAudio: мажорная фоновая петля, а слияние звучит аккордом, который поднимается с каждым тиром — арбуз выдаёт целую фанфару из 7 нот.

**Интеграция с ВК.** Имя игрока, прогресс в облаке `VKWebAppStorage`, рейтинг, пост рекорда на стену, вибро-отклик.

**Монетизация без сервера.** Кнопка «📺 ПРОДОЛЖИТЬ» на экране проигрыша: ролик за вознаграждение убирает верхнюю треть кучи и даёт доиграть. Это самый выгодный момент для показа — игрок только что проиграл и хочет продолжить. Настраивать ничего не нужно, ВК платит за показы.

**Вне ВКонтакте** игра полностью работает: VK-функции отключаются, реклама засчитывается сразу.

---

## Как менять под себя

- **Фрукты** — массив `FR`: `e` (эмодзи), `r` (радиус, доля ширины банки), `c` (цвет), `pts` (очки)
- **Что падает с неба** — `DROPPABLE` и функция `pickTier()` (вероятности)
- **Сложность** — `lineY` (высота красной черты), порог `1.9` сек в `checkOver()`
- **Физика** — `GRAV`, `SUB`, `ITER`, `DAMP`, `WALL_F`, `REST`
- **Порог слияния** — `d < rr*0.995` в `solve()` (чем меньше, тем труднее слить)
- **Музыка** — `schedStep()`, гамма `PENT`, аккорды `roots`
